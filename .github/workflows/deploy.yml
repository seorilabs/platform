# 플랫폼 서버 배포.
#
# 배포 대상이 다섯이고 전부 같은 이미지를 쓴다. 하나라도 빠지면 오래된
# 코드가 결제 경로 옆에서 돈다 — 실제로 겪었다. 2026-08-02에 서비스
# 넷은 p15인데 platform-worker만 p9f로 22시간 뒤처져 있었고, 그 사이
# ledger.go와 verify/service.go가 바뀌었다.
#
# 그래서 이 워크플로는 이미지를 한 번만 만들고 다섯 대상에 같은 태그로
# 올린 뒤, 실제로 같은 태그가 됐는지 다시 확인한다.
#
# ## 승인 게이트를 environment로 하지 않는 이유
#
# private repo에서 required reviewer는 이 요금제로 쓸 수 없다. API가
# 422로 거부한다("Please ensure the billing plan supports the required
# reviewers protection rule").
#
# 대신 배포 job을 workflow_dispatch에서만 돌게 한다. main 병합 시에는
# 빌드만 자동으로 돌고, 배포는 사람이 Actions에서 명시적으로 실행해야
# 시작된다. 실결제 원장이라 사람이 한 번 끊는다는 목적은 같다.
#
# 빌드 job은 같은 commit의 이미지가 이미 있으면 건너뛴다. 그래서 병합
# 직후 dispatch하면 빌드를 기다리지 않고 배포로 바로 들어간다.
#
# ## 러너
#
# GitHub-hosted를 기본으로 쓴다. 배포는 짧고, x64 러너에서는 amd64가
# 네이티브라 크로스컴파일도 필요 없다. ARC는 최대 3대라 짧은 작업이
# 자리를 차지하면 다른 작업을 밀어낸다. 조직 기본값인 ARC arm64 라우팅을
# 여기서만 의도적으로 벗어난다.
#
# GitHub Actions에 자동 fallback은 없다. 쿼타가 차면 명시적으로 넘긴다.
#
#   일회성  Run workflow의 runner 드롭다운에서 arc 선택
#   지속    저장소 변수 PLATFORM_DEPLOY_RUNNER=arc
#
# 둘 중 하나라도 arc면 ARC로 간다. 쿼타가 풀리면 변수를 지우면 된다.
name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      runner:
        description: "러너. GitHub 쿼타가 소진됐으면 arc를 고른다"
        type: choice
        required: false
        default: github
        options: [github, arc]
      iap_environment:
        description: "IAP 원장과 마켓 검증 환경. registry의 활성 IAP 앱과 같아야 한다"
        type: choice
        required: true
        default: production
        options: [production, sandbox]

permissions:
  contents: read
  id-token: write

# 배포가 겹치면 나중 것이 먼저 끝나 오래된 이미지가 남을 수 있다.
concurrency:
  group: deploy-platform
  cancel-in-progress: false

env:
  PROJECT_ID: seorilabs-platform
  REGION: asia-northeast3
  REGISTRY: asia-northeast3-docker.pkg.dev
  IMAGE_PATH: seorilabs-platform/platform/platform
  DEPLOYER_SA: platform-deployer@seorilabs-platform.iam.gserviceaccount.com
  # push는 build-only라 production 기본값을 쓴다. 실제 배포에서는
  # workflow_dispatch의 명시적 선택이 registry와 일치하는지 다시 검사한다.
  IAP_ENVIRONMENT: ${{ inputs.iap_environment || 'production' }}

jobs:
  build:
    name: Build and push image
    # GitHub-hosted에는 Docker가 이미 있고 x64라 amd64가 네이티브다.
    # ARC로 넘어가면 Docker가 필요하므로 DIND 라벨을 쓴다. 그쪽은
    # arm64지만 Dockerfile이 FROM --platform=$BUILDPLATFORM이라
    # QEMU 없이 amd64로 크로스컴파일한다.
    runs-on: ${{ (github.event.inputs.runner == 'arc' || vars.PLATFORM_DEPLOY_RUNNER == 'arc') && 'seorilabs-rpi-arm64-dind' || 'ubuntu-latest' }}
    timeout-minutes: 30
    outputs:
      image: ${{ steps.meta.outputs.image }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Resolve image reference
        id: meta
        run: echo "image=${REGISTRY}/${IMAGE_PATH}:${GITHUB_SHA}" >> "$GITHUB_OUTPUT"

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ vars.GOOGLE_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ env.DEPLOYER_SA }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v3

      # 병합 시 빌드해두고 dispatch로 배포하는 흐름이라, 같은 commit을
      # 두 번 빌드하게 된다. 이미 있으면 건너뛰어 승인 후 대기를 없앤다.
      - name: Check whether the image already exists
        id: exists
        run: |
          if gcloud artifacts docker images describe "${{ steps.meta.outputs.image }}" --quiet >/dev/null 2>&1; then
            echo "found=true" >> "$GITHUB_OUTPUT"
            echo "이미 있다. 빌드를 건너뛴다."
          else
            echo "found=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Configure Docker for Artifact Registry
        if: steps.exists.outputs.found == 'false'
        run: gcloud auth configure-docker "$REGISTRY" --quiet

      - name: Setup Buildx
        if: steps.exists.outputs.found == 'false'
        uses: docker/setup-buildx-action@v4

      - name: Build and push
        if: steps.exists.outputs.found == 'false'
        run: |
          docker buildx build \
            --platform linux/amd64 \
            --tag "${{ steps.meta.outputs.image }}" \
            --push \
            server

  deploy:
    name: Deploy production runtime
    needs: build
    # main 병합만으로는 배포하지 않는다. 사람이 Actions에서 실행해야
    # 시작된다 — 이것이 승인 게이트를 대신한다.
    if: github.event_name == 'workflow_dispatch'
    # gcloud만 쓰므로 Docker가 필요 없다. ARC로 넘어가도 일반 러너면 된다.
    runs-on: ${{ (github.event.inputs.runner == 'arc' || vars.PLATFORM_DEPLOY_RUNNER == 'arc') && 'seorilabs-rpi-arm64' || 'ubuntu-latest' }}
    timeout-minutes: 30
    # 보호 규칙은 못 걸지만 배포 이력이 environment에 남는다.
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      # 원장 prefix는 서비스 전체에서 하나다. 활성 IAP 앱이 서로 다른 환경을
      # 요구하거나 dispatch 선택과 registry가 다르면 일부 앱만 다른 원장을
      # 보게 되므로 배포 전에 닫는다.
      - name: Assert registry IAP environment
        run: |
          fail=0
          active=0
          for file in registry/apps/*.json; do
            enabled=$(jq -r '.features.iap // false' "$file")
            [ "$enabled" = "true" ] || continue

            active=$((active + 1))
            app_id=$(jq -r '.app_id' "$file")
            actual=$(jq -r '.iap.ledger_environment // ""' "$file")
            printf '%-24s ledger_environment=%s\n' "$app_id" "$actual"
            [ "$actual" = "$IAP_ENVIRONMENT" ] || fail=1
          done

          [ "$active" -gt 0 ] || { echo "활성 IAP 앱이 없다." >&2; exit 1; }
          [ "$fail" -eq 0 ] || {
            echo "활성 IAP 앱의 registry 환경과 배포 선택($IAP_ENVIRONMENT)이 다르다." >&2
            exit 1
          }

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ vars.GOOGLE_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ env.DEPLOYER_SA }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v3

      # 서비스 이미지와 IAP 원장 환경을 함께 고정한다. 환경을 명시하지 않으면
      # App Review 기간의 sandbox 값이 다음 배포에도 남아 registry와 런타임이
      # 갈라질 수 있다.
      - name: Deploy services
        run: |
          for s in platform-api platform-ingest; do
            echo "::group::$s"
            gcloud run deploy "$s" \
              --project="$PROJECT_ID" --region="$REGION" \
              --image="${{ needs.build.outputs.image }}" --quiet
            echo "::endgroup::"
          done

          gcloud run deploy platform-iap \
            --project="$PROJECT_ID" --region="$REGION" \
            --image="${{ needs.build.outputs.image }}" \
            --update-env-vars="IAP_LEDGER_ENVIRONMENT=${IAP_ENVIRONMENT},APPLE_APP_STORE_ENVIRONMENT=${IAP_ENVIRONMENT}" \
            --quiet

          gcloud run deploy platform-admin \
            --project="$PROJECT_ID" --region="$REGION" \
            --image="${{ needs.build.outputs.image }}" \
            --update-env-vars="IAP_LEDGER_ENVIRONMENT=${IAP_ENVIRONMENT}" \
            --quiet

      - name: Deploy worker job
        run: |
          gcloud run jobs update platform-worker \
            --project="$PROJECT_ID" --region="$REGION" \
            --image="${{ needs.build.outputs.image }}" \
            --update-env-vars="IAP_LEDGER_ENVIRONMENT=${IAP_ENVIRONMENT},APPLE_APP_STORE_ENVIRONMENT=${IAP_ENVIRONMENT}" \
            --quiet

      # 배포가 전부 성공했다고 같은 태그가 됐다는 보장은 없다.
      # 드리프트를 눈으로 확인하지 않으면 다음에 또 놓친다.
      - name: Assert all targets run the same image
        run: |
          expected="${{ needs.build.outputs.image }}"
          fail=0
          for s in platform-api platform-iap platform-ingest platform-admin; do
            actual=$(gcloud run services describe "$s" \
              --project="$PROJECT_ID" --region="$REGION" \
              --format='value(spec.template.spec.containers[0].image)')
            printf '%-18s %s\n' "$s" "$actual"
            [ "$actual" = "$expected" ] || { echo "  기대: $expected" >&2; fail=1; }
          done
          actual=$(gcloud run jobs describe platform-worker \
            --project="$PROJECT_ID" --region="$REGION" \
            --format='value(spec.template.spec.template.spec.containers[0].image)')
          printf '%-18s %s\n' "platform-worker" "$actual"
          [ "$actual" = "$expected" ] || { echo "  기대: $expected" >&2; fail=1; }
          [ "$fail" -eq 0 ] || { echo "배포 대상이 서로 다른 이미지로 돈다." >&2; exit 1; }

      - name: Assert selected IAP environment
        run: |
          service_env() {
            gcloud run services describe "$1" \
              --project="$PROJECT_ID" --region="$REGION" --format=json \
              | jq -r --arg name "$2" '.spec.template.spec.containers[0].env[] | select(.name == $name) | .value'
          }
          job_env() {
            gcloud run jobs describe "$1" \
              --project="$PROJECT_ID" --region="$REGION" --format=json \
              | jq -r --arg name "$2" '.spec.template.spec.template.spec.containers[0].env[] | select(.name == $name) | .value'
          }

          fail=0
          for target in platform-iap platform-admin; do
            actual=$(service_env "$target" IAP_LEDGER_ENVIRONMENT)
            printf '%-18s IAP_LEDGER_ENVIRONMENT=%s\n' "$target" "$actual"
            [ "$actual" = "$IAP_ENVIRONMENT" ] || fail=1
          done

          actual=$(service_env platform-iap APPLE_APP_STORE_ENVIRONMENT)
          printf '%-18s APPLE_APP_STORE_ENVIRONMENT=%s\n' platform-iap "$actual"
          [ "$actual" = "$IAP_ENVIRONMENT" ] || fail=1

          for name in IAP_LEDGER_ENVIRONMENT APPLE_APP_STORE_ENVIRONMENT; do
            actual=$(job_env platform-worker "$name")
            printf '%-18s %s=%s\n' platform-worker "$name" "$actual"
            [ "$actual" = "$IAP_ENVIRONMENT" ] || fail=1
          done

          [ "$fail" -eq 0 ] || { echo "선택한 IAP 환경($IAP_ENVIRONMENT)이 일치하지 않는다." >&2; exit 1; }

      # admin은 DRS로 비공개라 인증 없이는 403이 정상이다. 여기서
      # 확인하는 것은 공개 서비스 셋이 새 리비전으로 뜬 뒤 실제로
      # 응답하는지다. 부팅 실패는 ready에서 걸린다.
      - name: Smoke test
        run: |
          fail=0
          for s in api iap ingest; do
            url="https://platform-${s}-306278488979.${REGION}.run.app/health/ready"
            code=$(curl -s -o /dev/null -w '%{http_code}' --max-time 20 "$url" || echo 000)
            printf '%-8s %s\n' "$s" "$code"
            [ "$code" = "200" ] || fail=1
          done
          [ "$fail" -eq 0 ] || { echo "health/ready가 200이 아니다." >&2; exit 1; }
